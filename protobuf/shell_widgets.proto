syntax = "proto3";

// JS (shell) <-> Servo (widgets)

service WidgetState {
  rpc PushState (WidgetStateMsg) returns (Ack);
}

message WidgetStateMsg {
  string widget = 1;
  string json_payload = 2;
}

message Ack {
  bool ok = 1;
}
